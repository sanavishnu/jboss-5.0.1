<?xml version="1.0" encoding="UTF-8"?>
<server>
    <!-- Session of JavaMail resource -->
    <mbean code="org.jboss.mail.MailService"
           name="jboss:service=ElwisMail">
        <attribute name="JNDIName">java:/Elwis.Mail</attribute>
        <!--<attribute name="User">jboss</attribute> -->
        <!--<attribute name="Password">123456</attribute> -->

        <!-- jatun user-->
        <attribute name="User">miguel@jatun.com</attribute>
        <attribute name="Password">usr.000.usr</attribute>

        <!-- yahoo user-->
        <!--<attribute name="User">juanperezbo@yahoo.es</attribute>
        <attribute name="Password">123456</attribute>-->

        <attribute name="Configuration">
            <!-- Test -->
            <configuration>
                <!-- Change to your mail server prototocol -->
                <property name="mail.store.protocol" value="pop3"/>
                <property name="mail.transport.protocol" value="smtp"/>

                <!-- Change to the user who will receive mail  -->
                <property name="mail.user" value="nobody"/>

                <!-- Change to the mail server  -->
                <property name="mail.pop3.host" value="localhost"/>

                <!-- Change to the SMTP gateway server -->
                <!-- jatun smtp configuration-->
                <property name="mail.smtp.host" value="smtpout.secureserver.net"/>
                <property name="mail.smtp.auth" value="true"/>
                <property name="mail.smtp.port" value="25"/>


                <!-- yahoo smtp configuration-->
                <!--<property name="mail.smtp.host" value="smtp.mail.yahoo.com"/>
                <property name="mail.smtp.auth" value="true"/>
                <property name="mail.smtp.user" value="juanperezbo@yahoo.es" />
                <property name="mail.smtp.password" value="123456" />
                <property name="mail.smtp.ssl.enable" value="true" />
                <property name="mail.smtp.port" value="465 "/>-->


                <!-- Change to the address mail will be from  -->
                <!-- <property name="mail.from" value="jboss@localhost"/>-->

                <!-- Enable debugging output from the javamail classes -->
                <property name="mail.debug" value="false"/>
            </configuration>
        </attribute>
    </mbean>
</server>
